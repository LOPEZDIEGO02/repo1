const prompt = require('prompt-sync')();

const nombre   = prompt ("cual es tu nombre: ");
const edad     = prompt ("cuantos años tienes : ");
const estatura = prompt ("cuanto mides : ");
const peso     = prompt ("cual es tu peso corpotal")

if (edad > 10 && edad <100 ){
    console.log("tu nombre es", nombre)
}




let ipcion = Number(prompt("ingrsa un numero "))

switch (ipcion) {
    case 1: 
}